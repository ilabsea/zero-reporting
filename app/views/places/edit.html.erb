<% content_for(:title) { "#{@place.kind} Modification"} %>
<%= breadcrumb [ {"Health Structure" => users_path(place_id: @place.id)}, {"#{@place.kind} Modification" => nil}] %>

<%= page_header "#{@place.kind} Modification" %>

<%= render 'form' %>
