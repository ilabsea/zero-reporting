<% content_for(:title) { "New #{@place.kind}"} %>
<%= breadcrumb [ {"Health Structure" => users_path(place_id: @place.parent_id)}, {"#{@place.kind} Creation" => nil}] %>


<%= page_header "#{@place.kind} Creation" %>

<%= render 'form' %>
